# Project-level Gradle defaults
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

# Framework defaults (overridable via -Dxxx system properties)
env=dev
retry.count=2
thread.count=4
